<%# ✗ The red dot is the only "error" signal — no text or accessible name on the anchor, so SR users get nothing. %>
<%= ui :indicator, variant: :destructive do %>
  <span class="inline-flex size-9 items-center justify-center rounded-md bg-surface-sunken text-text-body" aria-hidden="true">!</span>
<% end %>
