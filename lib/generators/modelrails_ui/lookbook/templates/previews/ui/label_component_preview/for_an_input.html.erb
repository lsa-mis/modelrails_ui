<%# for: matches the input's id, so clicking the caption focuses the field %>
<div class="grid gap-2">
  <%= ui :label, "Email address", for: "user_email" %>
  <%= ui :input, type: "email", name: "email", id: "user_email" %>
</div>
