<%# Don't: a bare <input type="number"> skips AAA tokens, the 44px target, the %>
<%# focus ring, and the ARIA wiring. Use f.number_field or ui :number_input. %>
<input type="number" name="demo_raw" class="border px-3 py-1">
