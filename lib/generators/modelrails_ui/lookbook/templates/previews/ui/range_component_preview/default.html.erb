<%# A native slider labeled by an external <label for> targeting the emitted id. %>
<label for="demo_volume" class="block text-sm font-medium text-text-heading mb-1">Volume</label>
<%= ui :range, id: "demo_volume", name: "demo_volume", min: 0, max: 100, step: 1 %>
